(function(global){'use strict';
const VERSION='0.1-demo';
function n(v,d){const x=Number(v);return Number.isFinite(x)?x:d;}
function clamp(v,min,max){return Math.max(min,Math.min(max,n(v,min)));}
function evaluate(input){input=input||{};const capacity=input.capacity||{};const hireGate=input.hireGate||{};const metrics=input.metrics||{};const policy=input.policy||{};const requiredObservedRevenueMonths=Math.max(1,Math.floor(n(policy.requiredObservedRevenueMonths,3)));const requiredActiveBusinesses=Math.max(1,Math.floor(n(policy.requiredActiveBusinesses,10)));const requiredRetentionPct=clamp(policy.requiredRetentionPct==null?60:policy.requiredRetentionPct,0,100);const requiredCashCoverageMonths=clamp(policy.requiredCashCoverageMonths==null?3:policy.requiredCashCoverageMonths,0,24);const requiredGrowthDemandPct=clamp(policy.requiredGrowthDemandPct==null?20:policy.requiredGrowthDemandPct,0,100);const dataSource=String(metrics.dataSource||'DEMO').toUpperCase();const observedRevenueMonths=Math.max(0,Math.floor(n(metrics.observedRevenueMonths,0)));const activeBusinesses=Math.max(0,Math.floor(n(metrics.activeBusinesses,0)));const retentionPct=clamp(metrics.retentionPct==null?0:metrics.retentionPct,0,100);const monthlyOperatingResult=n(metrics.monthlyOperatingResult,0);const cashCoverageMonths=Math.max(0,n(metrics.cashCoverageMonths,0));const growthDemandPct=clamp(metrics.growthDemandPct==null?0:metrics.growthDemandPct,0,100);const legalCommercialReady=metrics.legalCommercialReady===true;const observedDataReady=dataSource==='OBSERVED';const reasons=[];if(!observedDataReady)reasons.push('OBSERVED_DATA_REQUIRED');if(!legalCommercialReady)reasons.push('LEGAL_COMMERCIAL_GATE_REQUIRED');if(observedRevenueMonths<requiredObservedRevenueMonths)reasons.push('INSUFFICIENT_OBSERVED_REVENUE_MONTHS');if(activeBusinesses<requiredActiveBusinesses)reasons.push('INSUFFICIENT_ACTIVE_BUSINESSES');if(retentionPct<requiredRetentionPct)reasons.push('RETENTION_BELOW_POLICY');if(!(monthlyOperatingResult>0))reasons.push('OPERATING_RESULT_NOT_POSITIVE');if(cashCoverageMonths<requiredCashCoverageMonths)reasons.push('CASH_COVERAGE_BELOW_POLICY');const consolidated=reasons.length===0;const growthOpportunity=consolidated&&growthDemandPct>=requiredGrowthDemandPct;const founderHireReview=hireGate.recommendation==='FOUNDER_REVIEW'||hireGate.readyForFounderReview===true||capacity.hiringSignal==='PREPARE_HIRE';let state='STAY_FOUNDER_LED';if(growthOpportunity)state='PREPARE_GROWTH_FINANCING';else if(founderHireReview)state='FOUNDER_REVIEW_FOR_HIRE';const capitalReasons=reasons.slice();if(consolidated&&growthDemandPct<requiredGrowthDemandPct)capitalReasons.push('GROWTH_DEMAND_BELOW_POLICY');return {version:VERSION,state,consolidated,growthOpportunity,founderHireReview,metrics:{dataSource,observedRevenueMonths,activeBusinesses,retentionPct,monthlyOperatingResult,cashCoverageMonths,growthDemandPct,legalCommercialReady},policy:{requiredObservedRevenueMonths,requiredActiveBusinesses,requiredRetentionPct,requiredCashCoverageMonths,requiredGrowthDemandPct},consolidationReasons:reasons,capitalPreparationReasons:capitalReasons,capitalPreparationOwners:['FOUNDER','AI_ASSIST'],capitalPreparationMode:'JOINT_AFTER_CONSOLIDATION',capitalResearchAllowed:consolidated,investorOutreachAllowed:false,investorOutreachRequiresFounderApproval:true,termAcceptanceOwner:'FOUNDER',debtAcceptanceOwner:'FOUNDER',dilutionAcceptanceOwner:'FOUNDER',autonomousCapitalCommitment:false,noFinancialCommitment:true,noExternalOutreach:true,externalApiCostEur:0,persistentStorage:false,containsPersonalData:false,assumptionOnly:dataSource!=='OBSERVED'};}
global.FIACOGrowthStrategy={version:VERSION,evaluate};
})(typeof window!=='undefined'?window:globalThis);
