(function(global){'use strict';
const VERSION='0.1-demo';
function safeNumber(v,min){const n=Number(v);return Number.isFinite(n)?Math.max(min||0,n):0;}
function toPortfolioItem(flow,meta){if(!global.FIACOEnterpriseOpsDashboard)throw new Error('Enterprise Ops no disponible');return global.FIACOEnterpriseOpsDashboard.fromFlow(flow,meta||{});}
function aggregate(items,period,referenceNow){if(!global.FIACOEnterpriseOpsDashboard)throw new Error('Enterprise Ops no disponible');const s=global.FIACOEnterpriseOpsDashboard.summarize(items||[],period||'ALL',referenceNow);const totalManualMinutes=s.humanMinutes+s.automatedMinutes;return {operations:s.count,automationPct:s.automationPct,totalManualMinutes,totalHumanMinutes:s.humanMinutes,escalationRatePct:s.escalationRatePct,riskEvents:s.riskEvents,containsPersonalData:false,persistentStorage:false,productionExecutionEnabled:false,noFinancialAuthority:true};}
function project(items,options){options=options||{};if(!global.FIACOPricing)throw new Error('Pricing no disponible');const report=aggregate(items,options.period||'ALL',options.referenceNow);if(report.operations<1)throw new Error('Falta muestra operativa');const bridge=global.FIACOPricing.pilotToRunwayOptions(report,{operationsM12:safeNumber(options.operationsM12||300,1),automationM12:safeNumber(options.automationM12||85,0)});const simulation=global.FIACOPricing.founderLedSimulation(Object.assign({},bridge,{dataSource:'UNIFIED_OPS_AGGREGATE',revenuePerManagement:safeNumber(options.revenuePerManagement||24,0),expenseM1:safeNumber(options.expenseM1||600,0),expenseM12:safeNumber(options.expenseM12||1325,0),initialCash:safeNumber(options.initialCash||0,0),founderShadowHourlyCost:safeNumber(options.founderShadowHourlyCost||0,0),humanOpsHourlyCashCost:safeNumber(options.humanOpsHourlyCashCost||0,0)}));return {report,bridge,simulation,containsPersonalData:false,persistentStorage:false,productionExecutionEnabled:false,noFinancialAuthority:true};}
global.FIACOOpsHub={version:VERSION,toPortfolioItem,aggregate,project};
})(typeof window!=='undefined'?window:globalThis);
