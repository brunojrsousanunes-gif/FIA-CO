name: Deploy mobile demo to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Configure Pages
        uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0
      - name: Verify Pages payload
        run: |
          test -f frontend/approved-home-preview.html
          test -f frontend/web-beta.html
          test -f frontend/beta-mobile.html
          test -f frontend/assets/fia-co-office-approved.jpg
          grep -Fq 'Conecta.' frontend/approved-home-preview.html
          grep -Fq 'Comprueba.' frontend/approved-home-preview.html
          grep -Fq 'FIA&CO' frontend/approved-home-preview.html
      - name: Stamp deployment version
        run: |
          printf '%s\n' "$GITHUB_SHA" > frontend/deploy-version.txt
      - name: Upload demo
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: frontend

  deploy:
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
