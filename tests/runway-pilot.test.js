const fs=require('fs');const assert=require('assert');const html=fs.readFileSync('frontend/runway-pilot.html','utf8');assert(/name="viewport"/.test(html));assert(/PILOT_AGGREGATE/.test(html));assert(/pilotToRunwayOptions/.test(html));assert(/humanOpsHourlyCashCost/.test(html));assert(!/company=|description=|email=|dni=|iban=/i.test(html));assert(!/https?:\/\/[^"']+\.js/i.test(html));console.log('runway pilot tests passed');