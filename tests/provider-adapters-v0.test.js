const fs=require('fs');const vm=require('vm');const assert=require('assert');const code=fs.readFileSync('frontend/js/provider-adapters.js','utf8');const sandbox={window:{}};vm.createContext(sandbox);vm.runInContext(code,sandbox);const api=sandbox.window.FIAProviderAdapters;assert.strictEqual(api.DEMO_ONLY,true);assert.strictEqual(api.get('payments').mode,'MOCK_ONLY');assert.ok(api.get('payments').capabilities.includes('quote'));(async()=>{const r=await api.get('fraud').execute('explain',{scenario:'missing'});assert.strictEqual(r.status,'MOCK_ONLY');assert.strictEqual(r.demo,true);let failed=false;try{await api.get('fraud').execute('block',{});}catch(e){failed=true;}assert.strictEqual(failed,true);console.log('provider adapters v0 tests passed');})().catch(e=>{console.error(e);process.exit(1);});